// API provider placeholder — activate when FastAPI backend is ready.
// To use: uncomment and set baseUrl to your backend address (e.g. http://localhost:8000)
//
// Auth endpoints:
//   POST /auth/signup   → SignUpRequest → TokenResponse
//   POST /auth/login    → LoginRequest  → TokenResponse
//   POST /auth/logout   → 204
//
// Notes endpoints:
//   GET    /notes        → List[NoteResponse]
//   GET    /notes/{id}   → NoteResponse | 404
//   POST   /notes        → NoteResponse (201)
//   PATCH  /notes/{id}   → NoteResponse | 404
//   DELETE /notes/{id}   → 204 | 404

class ApiProvider {
  // TODO: activate with your backend URL when ready
  // static const baseUrl = 'http://localhost:8000';
}
